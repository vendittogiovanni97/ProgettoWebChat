/*import { EmailManager } from '../..types/EmailManager'
import { v4 } from 'uuid';

interface ResetPasswordInfo {
  email: string;
  username? : string;   //meglio da email?
  token: string;
}



async function resetPassword(req: Request, res: Response) {
  const { email } = req.body;
  const user = await*/